# Release Notes v2.6.2

## 🚀 모바일 원격 연동 고유 다이렉트 터널 감지 및 'No Host' 이슈 완전 해결

스마트폰으로 모바일 원격 제어 QR 코드를 스캔했을 때 호스트 PC를 찾지 못하고 `>< No Host` 빈 화면에 머무르던 문제를 근본적으로 해결했습니다.

### 주요 변경 사항

1. **내 PC 전용 다이렉트 터널 URI (`vscode.dev/tunnel/<호스트>/...`) 동적 감지**
   - 기존의 정적 공용 주소(`vscode.dev/agents`)를 전면 폐기했습니다.
   - VS Code 내장 Remote Tunnel API(`workbench.action.remoteTunnel.copyBrowserUrl`)를 연동하여 내 PC에 발급된 고유 터널 주소를 자동으로 읽어옵니다.
   - 읽어온 다이렉트 주소로 전용 QR 코드를 동적으로 렌더링하여, 스마트폰으로 찍는 즉시 호스트 선택 없이 내 PC 워크스페이스로 직행합니다.

2. **모바일 웹뷰 3단계 동적 상태머신 적용**
   - **상태 1 (⚪ 원격 터널 대기 중):** 터널이 켜져 있지 않을 때 상태 뱃지 및 안내를 표시하고, [원격 터널 켜기] 버튼을 통해 1클릭 시작 지원.
   - **상태 2 (진행 안내):** VS Code 상단 GitHub/Microsoft 로그인 팝업 확인 안내 상시 노출.
   - **상태 3 (🟢 다이렉트 연결 완료):** 다이렉트 URL 감지 즉시 초록색 뱃지 전환 및 내 PC 전용 QR 코드로 실시간 교체.
   - [🔄 연결 상태 새로고침] 버튼을 제공하여 언제든 최신 터널 상태를 동기화할 수 있습니다.

3. **폴링 타이머 자원 누수 방지 및 라이프사이클 안전성**
   - 터널 활성화 대기 폴링 타이머에 30회 상한을 적용하고, 웹뷰 닫힘(`onDidDispose`) 및 익스텐션 비활성화 시 즉시 해제하도록 리소스 누수를 완벽 차단했습니다.

4. **단위 및 통합 테스트 완료**
   - `tests/mobile.test.cjs`: 다이렉트 터널 URL 생성, 미활성화 대기 UI, 활성화 다이렉트 QR 렌더링에 대한 3개 테스트 케이스 추가 및 통과.
